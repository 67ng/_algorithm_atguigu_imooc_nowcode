package algorithm.trick.prefix_suffix_diff;

/**
 * @Description: 树上前缀
 * 设SUM(i)表示节点i到根节点的权值总和，则有：
 * 1.若是点权，则x，y路径上的和为SUM(x)+SUM(y)-SUM(lca)-SUM(Fa(lca))
 * 2.若是边权，则x，y路径上的和为SUM(x)+SUM(y)-2*SUM(lca)
 * 其中，lca表示最近公共祖先节点
 * @Date: 2022/1/12
 */

public class TreePrefix {

}
